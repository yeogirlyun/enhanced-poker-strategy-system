# Relevant source code from chip_animations.py

...existing code...

# Example of chip animations
class ChipAnimations:
    def animate_chips_to_pot(self, player_id, amount):
        print(f"Animating {amount} chips from player {player_id} to pot")

    def animate_pot_to_winner(self, winner_id, amount):
        print(f"Animating {amount} chips from pot to winner {winner_id}")

...existing code...
